jamboard-app_開発ノート
==================================================
■ 2026/09/20【STEP 4完了：Vercel Web公開 ＆ トラブルシューティング完了】
1. プロジェクト基本情報
アプリ名: jamboard-app
GitHubユーザー名: t00714fh-design
リポジトリURL: https://github.com/t00714fh-design/jamboard-app
Vercel 公開URL: https://jamboard-app-sixq.vercel.app
Firebase プロジェクト名: jamboard-app-48edd
使用ブランチ: main
現在のステータス:
STEP 1（ローカル開発環境構築）完了
STEP 2（GitHub連携 ＆ Chromebook引き継ぎ環境構築）完了
STEP 3（Firebase Realtime Databaseを用いた複数端末リアルタイム同期機能）完了
STEP 4（Vercelへの無料デプロイ・Web公開とトラブルシューティング）完了
次の予定: 学校環境での閲覧許可申請（ホワイトリスト登録）および Google Classroom での児童生徒への配布・授業活用
2. 新しいGeminiチャットへの引き継ぎ用プロンプト
※新しいチャットを開始する際は、以下の枠内を全選択して送信してください。
以下の【開発引き継ぎノート】を読み込んで、開発のサポートをお願いします。

1. リポジトリ・進捗情報
* リポジトリURL: https://github.com/t00714fh-design/jamboard-app
* Vercel WebアプリURL: https://jamboard-app-sixq.vercel.app
* ブランチ: main
* 進捗状態: STEP 1〜4 までのコード実装・Web公開・同期動作確認がすべて完了。

2. 完了したステップ
* STEP 1: ローカル開発環境構築（Vite + React + TypeScript + tldraw）
  全画面ホワイトボード化・描画機能の実装完了。
* STEP 2: GitHub連携 ＆ Chromebook引き継ぎ環境の確立
  mainブランチへのPush完了、GitHub Codespacesでの開発環境構築完了。
* STEP 3: Firebase Realtime Database を用いたリアルタイム共同作業機能の実装
  src/firebase.ts（接続設定・databaseURL設定）の作成完了。
  src/App.tsx にて onValue / listen を用いた同期ロジック実装完了。
  スナップショットを JSON.stringify / JSON.parse で文字列化してFirebaseの記号制限を回避。
* STEP 4: Vercelへの無料デプロイ ＆ Web公開・動作確認
  GitHubとVercelを連携し、自動ビルド・デプロイ環境を確立。
  完全な apiKey と appId を設定し、複数端末でのリアルタイム相互同期の動作確認完了。

3. 現在の状況・次の予定
* 現在の状況: アプリの全コア機能（ホワイトボード描画、付箋、リアルタイム共同編集、Web公開）が完成・稼働中。
* 次の作業予定: 授業での活用（Google Classroomでの配信）、追加機能の検討（画像保存・清書機能など）、学校ネットワークでのフィルタリング解除申請。

4. Chromebook（GitHub Codespaces）での開発再開手順
1. Chromeブラウザで https://github.com/t00714fh-design/jamboard-app を開く
2. 緑色の「<> Code」ボタン ＞「Codespaces」タブ ＞「Create codespace on main」を選択
3. 画面下のターミナルで npm run dev を実行
4. 右下のポップアップから「ブラウザで開く」をクリックして動作確認

【作業保存手順】
作業終了時はターミナルで以下を実行：
git add .
git commit -m "作業内容のメモ"
git push

【Geminiへの依頼】
上記の前提知識を把握した上で、学校での授業活用サポートや、今後の機能追加（画像エクスポート、キャンバスクリア、部屋分け機能など）の提案・サポートを開始してください。


3. 日常作業コマンド集
【作業終了時（GitHubへ保存）】
git add .
git commit -m "作業内容のメモ"
git push


【作業開始時（最新データを取得・開発サーバー起動）】
git pull
npm run dev


==================================================
■ 過去の対応・トラブルシューティング履歴
【環境・Git関連】
GUI自動公開衝突: ターミナルでの手動コマンド送信へ切り替え。
Gitユーザー未設定: git config --global で email/name を登録。
ブランチ名統一: master から main へ変更（git branch -M main）。
初回Push完了: git add . → git commit → git push -u origin main でGitHub上にコード一覧を反映。
【STEP 3：Firebase & tldraw リアルタイム同期関連】
TypeScript型エラー（loadSnapshot / getSnapshot）:
editor.store.loadSnapshot() ではなく、tldraw モジュールから loadSnapshot(editor.store, data) および getSnapshot(editor.store) をインポートして呼び出す形に修正して解消。
Firebaseデータ送信不能（null のまま変わらない現象）:
tldraw のスナップショットオブジェクト内にFirebaseで不許可とされる特殊文字・ネスト構造が含まれていたため、JSON.stringify(snapshot) で文字列化して set() 保存、読み込み時に JSON.parse(data) する方式へ変更し、同期成功。
Firebaseセキュリティルール:
テスト環境として Realtime Database のルールを .read: true, .write: true に設定・公開。
【STEP 4：Vercel公開・ネットワーク環境トラブル関連】
Vercel公開後に画面が真っ白になる現象:
原因: src/firebase.ts 内の apiKey および appId の文字列に省略記号（...）が残っており、接続エラーが発生していたため。
対処: Firebaseコンソール（プロジェクトの設定 > マイアプリ）より完全な文字列を取得して firebase.ts を修正し、git push を実行してデプロイを自動更新。
学校PC（Chrome）で「ページがブロックされています」と表示される現象:
原因: 学校指定のフィルタリングソフト（InterCLASS Filtering Service）が Vercel ドメイン（.vercel.app）を「カテゴリ: ホスティング」として一括自動遮断していたため（プログラムのバグではない）。
回避策: フィルタリング拡張機能が未適用の Microsoft Edge ブラウザでは正常動作を確認。
恒久対策: 学校のICT担当者および教育委員会へ、当該URL（https://jamboard-app-sixq.vercel.app）のホワイトリスト（閲覧許可）登録を申請する。
